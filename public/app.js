const easySeeds = [
  ["Science", "Tides and orbits?", "Gravity"],
  ["Science", "Olympus Mons planet?", "Mars"],
  ["Science", "Plants absorb this gas?", "Carbon dioxide"],
  ["Science", "Blood-pumping organ?", "The heart"],
  ["Science", "Negative atom particle?", "Electron"],
  ["Science", "Brain, spine, nerves?", "The nervous system"],
  ["Science", "Brain's bone case?", "The skull"],
  ["Science", "Grooved wheel lifter?", "A pulley"],
  ["Science", "Plants and meat eater?", "Omnivore"],
  ["Science", "Leaf sugar process?", "Photosynthesis"],
  ["History", "Valley Forge leader?", "George Washington"],
  ["History", "Pyramids and hieroglyphs?", "Ancient Egypt"],
  ["History", "Cold War wall city?", "Berlin"],
  ["History", "Legions and Latin?", "The Roman Empire"],
  ["History", "Flying sketches artist?", "Leonardo da Vinci"],
  ["History", "D-Day global war?", "World War II"],
  ["History", "Pilgrims' 1620 ship?", "The Mayflower"],
  ["History", "Statue donor country?", "France"],
  ["History", "Caesar and Antony queen?", "Cleopatra"],
  ["History", "Emancipation president?", "Abraham Lincoln"],
  ["Geography", "California to Japan ocean?", "The Pacific Ocean"],
  ["Geography", "Gondolas and canals?", "Venice"],
  ["Geography", "Luxor and Cairo river?", "The Nile"],
  ["Geography", "Seine and Louvre city?", "Paris"],
  ["Geography", "Amazon and Portuguese?", "Brazil"],
  ["Geography", "Khumbu summit?", "Mount Everest"],
  ["Geography", "Shibuya capital?", "Tokyo"],
  ["Geography", "Madrid and Seville?", "Spain"],
  ["Geography", "Ireland to Newfoundland ocean?", "The Atlantic Ocean"],
  ["Geography", "Siberia country?", "Russia"],
  ["Literature", "Hamlet and Juliet writer?", "William Shakespeare"],
  ["Literature", "Four-founder magic school?", "Hogwarts"],
  ["Literature", "Piglet's honey friend?", "Winnie-the-Pooh"],
  ["Literature", "Grinch creator?", "Dr. Seuss"],
  ["Literature", "Midnight slipper tale?", "Cinderella"],
  ["Literature", "McGregor garden rabbit?", "Peter Rabbit"],
  ["Literature", "Willy Wonka author?", "Roald Dahl"],
  ["Literature", "Grandmother disguise villain?", "The Big Bad Wolf"],
  ["Literature", "Lonely Mountain burglar?", "Bilbo Baggins"],
  ["Literature", "Baker Street detective?", "Sherlock Holmes"],
  ["Film & TV", "Simba exile film?", "The Lion King"],
  ["Film & TV", "Neo simulation film?", "The Matrix"],
  ["Film & TV", "742 Evergreen Terrace show?", "The Simpsons"],
  ["Film & TV", "Arendelle snowman?", "Olaf"],
  ["Film & TV", "Andy and Buzz film?", "Toy Story"],
  ["Film & TV", "Daily Bugle hero?", "Spider-Man"],
  ["Film & TV", "Gotham signal hero?", "Batman"],
  ["Film & TV", "Jedi and Tatooine saga?", "Star Wars"],
  ["Film & TV", "Swamp ogre film?", "Shrek"],
  ["Film & TV", "Grey wizard guide?", "Gandalf"],
  ["Music", "Chin-held bowed string?", "A violin"],
  ["Music", "Moonwalk glove singer?", "Michael Jackson"],
  ["Music", "Hammered keyboard strings?", "A piano"],
  ["Music", "Abbey Road band?", "The Beatles"],
  ["Music", "Six-string rock instrument?", "A guitar"],
  ["Music", "Snare, toms, cymbals?", "Drums"],
  ["Music", "Music speed term?", "Tempo"],
  ["Music", "SATB singing group?", "A choir"],
  ["Music", "Hello singer?", "Adele"],
  ["Music", "Voice below baritone?", "Bass"],
  ["Sports", "Soccer starting side count?", "Eleven"],
  ["Sports", "Shuttlecock sport?", "Badminton"],
  ["Sports", "Foul-line basketball shot?", "A free throw"],
  ["Sports", "Innings and mound sport?", "Baseball"],
  ["Sports", "All England grass event?", "Wimbledon"],
  ["Sports", "Olympic rings count?", "Five"],
  ["Sports", "Puck and penalty box?", "Ice hockey"],
  ["Sports", "Six-point football score?", "A touchdown"],
  ["Sports", "One under par?", "A birdie"],
  ["Sports", "Soccer hands position?", "Goalkeeper"],
  ["Technology", "Central processor acronym?", "CPU"],
  ["Technology", "Cursor-clicking device?", "A mouse"],
  ["Technology", "Web layout language?", "CSS"],
  ["Technology", "Satellite navigation acronym?", "GPS"],
  ["Technology", "Keyboard device type?", "Input device"],
  ["Technology", "Home router wireless?", "Wi-Fi"],
  ["Technology", "Canned-meat email?", "Spam"],
  ["Technology", "Username's secret partner?", "A password"],
  ["Technology", "Type-A and Type-C family?", "USB"],
  ["Technology", "Social tag symbol?", "Hashtag"],
  ["Food", "Vinegared rice dish?", "Sushi"],
  ["Food", "Dried grapes?", "Raisins"],
  ["Food", "Pre-lunch morning meal?", "Breakfast"],
  ["Food", "Tahini legume dip?", "Hummus"],
  ["Food", "Dense Italian frozen dessert?", "Gelato"],
  ["Food", "Stretchy pizza cheese?", "Mozzarella"],
  ["Food", "Pressure-pulled coffee?", "Espresso"],
  ["Food", "Dill spear vegetable?", "Cucumber"],
  ["Food", "Bread flour grain?", "Wheat"],
  ["Food", "Cider pie fruit?", "Apple"],
  ["Nature", "Bee flower liquid?", "Nectar"],
  ["Nature", "Panda grass?", "Bamboo"],
  ["Nature", "Tadpole amphibian?", "A frog"],
  ["Nature", "Wolf group?", "A pack"],
  ["Nature", "Acorn tree?", "An oak tree"],
  ["Nature", "Caterpillar adult insect?", "Butterfly"],
  ["Nature", "Tusks and trunk mammal?", "An elephant"],
  ["Nature", "Striped horse relative?", "A zebra"],
  ["Nature", "Plant sugar process?", "Photosynthesis"],
  ["Nature", "Pond quacking bird?", "A duck"],
];

const mediumSeeds = [
  ["Science", "Na in table salt?", "Sodium"],
  ["Science", "Acid-base number scale?", "The pH scale"],
  ["Science", "Lymphocyte cell type?", "White blood cells"],
  ["Science", "Cell respiration organelles?", "Mitochondria"],
  ["Science", "Two H, one O?", "Water"],
  ["Science", "Surface vapor change?", "Evaporation"],
  ["Science", "Titan's ringed planet?", "Saturn"],
  ["Science", "Food-bond energy?", "Chemical energy"],
  ["Science", "Mohs 10 mineral?", "Diamond"],
  ["Science", "Chromosome holder?", "The nucleus"],
  ["History", "VE Day year?", "1945"],
  ["History", "Mansa Musa empire?", "The Mali Empire"],
  ["History", "Bastille revolution?", "The French Revolution"],
  ["History", "Byzantium later Istanbul?", "Constantinople"],
  ["History", "Hall of Mirrors treaty?", "The Treaty of Versailles"],
  ["History", "Actium victor emperor?", "Augustus"],
  ["History", "Silk and paper route?", "The Silk Road"],
  ["History", "Armada Tudor queen?", "Elizabeth I"],
  ["History", "1492 Spain navigator?", "Christopher Columbus"],
  ["History", "Black Tuesday crash?", "The Wall Street Crash"],
  ["Geography", "Cook Strait capital?", "Wellington"],
  ["Geography", "Kilimanjaro country?", "Tanzania"],
  ["Geography", "Atlantic-Mediterranean strait?", "The Strait of Gibraltar"],
  ["Geography", "Vienna-Budapest river?", "The Danube"],
  ["Geography", "Palermo island?", "Sicily"],
  ["Geography", "Gatineau-adjacent capital?", "Ottawa"],
  ["Geography", "Mauritania-to-Egypt desert?", "The Sahara"],
  ["Geography", "Europe's eastern edge range?", "The Ural Mountains"],
  ["Geography", "Han River capital?", "Seoul"],
  ["Geography", "Casablanca country?", "Morocco"],
  ["Literature", "Elizabeth Bennet creator?", "Jane Austen"],
  ["Literature", "Lonely Mountain dragon?", "Smaug"],
  ["Literature", "Ahab's whale novel?", "Moby-Dick"],
  ["Literature", "Poirot creator?", "Agatha Christie"],
  ["Literature", "Odysseus home epic?", "The Odyssey"],
  ["Literature", "Lake Geneva monster author?", "Mary Shelley"],
  ["Literature", "Room 101 novel?", "1984"],
  ["Literature", "West Egg novel?", "The Great Gatsby"],
  ["Literature", "Jo and Meg surname?", "March"],
  ["Literature", "Macondo author?", "Gabriel Garcia Marquez"],
  ["Film & TV", "Jaws dinosaur director?", "Steven Spielberg"],
  ["Film & TV", "Friends coffee shop?", "Central Perk"],
  ["Film & TV", "Jack Sparrow actor?", "Johnny Depp"],
  ["Film & TV", "Han Solo ship?", "The Millennium Falcon"],
  ["Film & TV", "Spirited Away director?", "Hayao Miyazaki"],
  ["Film & TV", "Clark Kent newspaper?", "The Daily Planet"],
  ["Film & TV", "Rick's cafe film?", "Casablanca"],
  ["Film & TV", "Shrimp-boat Woody actor?", "Tom Hanks"],
  ["Film & TV", "Hawkins disappearance series?", "Stranger Things"],
  ["Film & TV", "Vibranium kingdom?", "Wakanda"],
  ["Music", "Fifth Symphony composer?", "Ludwig van Beethoven"],
  ["Music", "Exodus reggae singer?", "Bob Marley"],
  ["Music", "Trumpet family?", "Brass"],
  ["Music", "Queen Live Aid frontman?", "Freddie Mercury"],
  ["Music", "Raises pitch semitone?", "A sharp"],
  ["Music", "Vogue singer?", "Madonna"],
  ["Music", "Gradually louder?", "Crescendo"],
  ["Music", "Dark Side band?", "Pink Floyd"],
  ["Music", "Magic Flute composer?", "Wolfgang Amadeus Mozart"],
  ["Music", "Violin-cello middle string?", "A viola"],
  ["Sports", "Pele yellow shirts?", "Brazil"],
  ["Sports", "Cassius Clay name?", "Muhammad Ali"],
  ["Sports", "Paris cycling race?", "The Tour de France"],
  ["Sports", "Jordan's 90s team?", "The Chicago Bulls"],
  ["Sports", "All bases one hit?", "A home run"],
  ["Sports", "Skiing plus rifle?", "Biathlon"],
  ["Sports", "Swiss one-handed backhand?", "Roger Federer"],
  ["Sports", "Scrums and tries?", "Rugby"],
  ["Sports", "Swim-bike-run race?", "Triathlon"],
  ["Sports", "Augusta green jacket?", "The Masters"],
  ["Technology", "Web structure acronym?", "HTML"],
  ["Technology", "Garage macOS company?", "Apple"],
  ["Technology", "SELECT JOIN language?", "SQL"],
  ["Technology", "Remote copy recovery?", "Backup"],
  ["Technology", "Android server kernel?", "Linux"],
  ["Technology", "Uniform web address acronym?", "URL"],
  ["Technology", "Cryptographic block ledger?", "Blockchain"],
  ["Technology", "Encrypted HTTP?", "HTTPS"],
  ["Technology", "Viruses ransomware umbrella?", "Malware"],
  ["Technology", "Power-cleared memory?", "RAM"],
  ["Food", "Yellow curry spice?", "Turmeric"],
  ["Food", "Basil pine-nut sauce?", "Pesto"],
  ["Food", "Creamy risotto rice?", "Arborio rice"],
  ["Food", "Folded tortilla food?", "Taco"],
  ["Food", "Spicy Korean cabbage?", "Kimchi"],
  ["Food", "Tofu bean?", "Soybean"],
  ["Food", "Laminated crescent pastry?", "Croissant"],
  ["Food", "Chickpea fried balls?", "Falafel"],
  ["Food", "Greek salad cheese?", "Feta"],
  ["Food", "Mascarpone coffee dessert?", "Tiramisu"],
  ["Nature", "Permafrost biome?", "Tundra"],
  ["Nature", "Frogs and newts group?", "Amphibians"],
  ["Nature", "Eight-arm ink animal?", "An octopus"],
  ["Nature", "Pines and firs group?", "Conifers"],
  ["Nature", "Pole-to-pole migrant bird?", "The Arctic tern"],
  ["Nature", "Coral-built ecosystem?", "A coral reef"],
  ["Nature", "Rosette big cat?", "A leopard"],
  ["Nature", "Combustion respiration gas?", "Oxygen"],
  ["Nature", "Seismograph event?", "An earthquake"],
  ["Nature", "Waggle dance insect?", "A honeybee"],
];
function buildQuestions(seeds, count, difficulty) {
  return Array.from({ length: count }, (_, index) => {
    const [category, prompt, answer] = seeds[index % seeds.length];
    return {
      answer,
      category,
      difficulty,
      id: `${difficulty}-${index + 1}`,
      prompt,
    };
  });
}

const questions = [...buildQuestions(easySeeds, 300, "easy"), ...buildQuestions(mediumSeeds, 200, "medium")];
const state = {
  answeredIds: new Set(),
  badStreak: 0,
  currentQuestion: null,
  goodStreak: 0,
  mode: "question",
  result: null,
  resultTimer: null,
  timer: null,
  timerEnded: false,
  timerRunning: false,
  timerRemaining: 5,
};
const settings = {
  correctSound: "chime",
  punishmentBase: 1,
  punishmentLengthBase: 10,
  punishmentLengthStep: 12,
  punishmentScaling: "accelerated",
  punishmentStep: 1,
  questionSeconds: 5,
  rewardBase: 10,
  rewardScaling: "accelerated",
  rewardStep: 12,
  timerEnabled: true,
  timerSound: "alarm",
  timerSoundEnabled: true,
  wrongSound: "buzz",
};
const nodes = {
  answer: document.getElementById("answerText"),
  bankSummary: document.getElementById("bankSummary"),
  correctButton: document.getElementById("correctButton"),
  correctSound: document.getElementById("correctSound"),
  difficulty: document.getElementById("questionDifficulty"),
  outcomeLabel: document.getElementById("outcomeLabel"),
  outcomePanel: document.getElementById("outcomePanel"),
  outcomeValue: document.getElementById("outcomeValue"),
  position: document.getElementById("questionPosition"),
  progress: document.getElementById("progressCount"),
  prompt: document.getElementById("questionPrompt"),
  punishmentBase: document.getElementById("punishmentBase"),
  punishmentLengthBase: document.getElementById("punishmentLengthBase"),
  punishmentLengthStep: document.getElementById("punishmentLengthStep"),
  punishmentScaling: document.getElementById("punishmentScaling"),
  punishmentStep: document.getElementById("punishmentStep"),
  questionSeconds: document.getElementById("questionSeconds"),
  rewardBase: document.getElementById("rewardBase"),
  rewardScaling: document.getElementById("rewardScaling"),
  rewardStep: document.getElementById("rewardStep"),
  settingsForm: document.getElementById("settingsForm"),
  skipButton: document.getElementById("skipButton"),
  startTimerButton: document.getElementById("startTimerButton"),
  nextButton: document.getElementById("nextButton"),
  timer: document.getElementById("timerDisplay"),
  timerEnabled: document.getElementById("timerEnabled"),
  timerSound: document.getElementById("timerSound"),
  timerSoundEnabled: document.getElementById("timerSoundEnabled"),
  wrongButton: document.getElementById("wrongButton"),
  wrongSound: document.getElementById("wrongSound"),
};
let audioContext;

function getNumber(node, fallback, min, max) {
  const value = Number(node.value);
  const clamped = Number.isFinite(value) ? Math.min(max, Math.max(min, value)) : fallback;
  node.value = clamped;
  return clamped;
}

function readSettings() {
  settings.rewardBase = getNumber(nodes.rewardBase, 10, 1, 600);
  settings.rewardStep = getNumber(nodes.rewardStep, 12, 0, 300);
  settings.rewardScaling = nodes.rewardScaling.value;
  settings.punishmentBase = getNumber(nodes.punishmentBase, 1, 1, 99);
  settings.punishmentStep = getNumber(nodes.punishmentStep, 1, 0, 20);
  settings.punishmentScaling = nodes.punishmentScaling.value;
  settings.punishmentLengthBase = getNumber(nodes.punishmentLengthBase, 10, 1, 600);
  settings.punishmentLengthStep = getNumber(nodes.punishmentLengthStep, 12, 0, 300);
  settings.correctSound = nodes.correctSound.value;
  settings.wrongSound = nodes.wrongSound.value;
  settings.timerSound = nodes.timerSound.value;
  settings.questionSeconds = getNumber(nodes.questionSeconds, 5, 5, 600);
  settings.timerEnabled = nodes.timerEnabled.checked;
  settings.timerSoundEnabled = nodes.timerSoundEnabled.checked;
}

function growth(streak, step, scaling) {
  const extra = Math.max(0, streak - 1);
  if (scaling === "flat") return 0;
  if (scaling === "accelerated") return (extra * (extra + 1) * step) / 2;
  return extra * step;
}

function rewardFor(streak) {
  return Math.round(settings.rewardBase + growth(streak, settings.rewardStep, settings.rewardScaling));
}

function punishmentFor(streak) {
  return {
    intensity: Math.round(settings.punishmentBase + growth(streak, settings.punishmentStep, settings.punishmentScaling)),
    length: Math.round(settings.punishmentLengthBase + growth(streak, settings.punishmentLengthStep, settings.punishmentScaling)),
  };
}

function ensureAudio() {
  if (!audioContext) {
    const AudioEngine = window.AudioContext || window.webkitAudioContext;
    audioContext = new AudioEngine();
  }
  if (audioContext.state === "suspended") {
    audioContext.resume();
  }
  return audioContext;
}

function tone(frequency, start, duration, type = "sine", gain = 0.12) {
  const context = ensureAudio();
  const osc = context.createOscillator();
  const volume = context.createGain();
  osc.type = type;
  osc.frequency.setValueAtTime(frequency, context.currentTime + start);
  volume.gain.setValueAtTime(gain, context.currentTime + start);
  volume.gain.exponentialRampToValueAtTime(0.001, context.currentTime + start + duration);
  osc.connect(volume);
  volume.connect(context.destination);
  osc.start(context.currentTime + start);
  osc.stop(context.currentTime + start + duration);
}

function playSound(name) {
  if (name === "off") return;
  if (name === "chime") {
    tone(660, 0, 0.12);
    tone(990, 0.1, 0.18);
  } else if (name === "blip") {
    tone(880, 0, 0.1, "square", 0.08);
  } else if (name === "click") {
    tone(1200, 0, 0.04, "square", 0.06);
  } else if (name === "buzz") {
    tone(140, 0, 0.28, "sawtooth", 0.12);
  } else if (name === "thud") {
    tone(80, 0, 0.18, "sine", 0.16);
  } else if (name === "drop") {
    tone(320, 0, 0.1, "triangle", 0.12);
    tone(120, 0.1, 0.2, "triangle", 0.1);
  } else if (name === "alarm") {
    tone(720, 0, 0.12, "square", 0.1);
    tone(720, 0.2, 0.12, "square", 0.1);
  } else if (name === "double") {
    tone(540, 0, 0.1);
    tone(540, 0.16, 0.1);
  } else if (name === "bell") {
    tone(1040, 0, 0.5, "sine", 0.08);
  }
}

function formatSeconds(total) {
  const minutes = Math.floor(total / 60);
  const seconds = total % 60;
  return `${String(minutes).padStart(2, "0")}:${String(seconds).padStart(2, "0")}`;
}

function stopTimer() {
  if (state.timer) {
    clearInterval(state.timer);
    state.timer = null;
  }
  state.timerRunning = false;
}

function stopResultTimer() {
  if (state.resultTimer) {
    clearInterval(state.resultTimer);
    state.resultTimer = null;
  }
}

function setQuestionTimerIdle() {
  stopTimer();
  state.timerEnded = false;
  state.timerRemaining = settings.questionSeconds;
  nodes.timer.classList.remove("ended");
  nodes.timer.textContent = settings.timerEnabled ? "Start" : "Timer off";
  nodes.startTimerButton.disabled = !settings.timerEnabled;
}

function startTimer() {
  if (state.mode !== "question" || state.timerRunning) return;
  stopTimer();
  state.timerEnded = false;
  state.timerRunning = true;
  state.timerRemaining = settings.questionSeconds;
  nodes.timer.classList.remove("ended");
  nodes.timer.textContent = settings.timerEnabled ? formatSeconds(state.timerRemaining) : "Timer off";
  if (!settings.timerEnabled) return;
  state.timer = setInterval(() => {
    state.timerRemaining -= 1;
    nodes.timer.textContent = formatSeconds(Math.max(0, state.timerRemaining));
    if (state.timerRemaining <= 0) {
      stopTimer();
      state.timerEnded = true;
      nodes.timer.classList.add("ended");
      if (settings.timerSoundEnabled) playSound(settings.timerSound);
    }
  }, 1000);
}

function randomQuestion(excludeId = null) {
  let pool = questions.filter((question) => !state.answeredIds.has(question.id));
  if (pool.length === 0) {
    state.answeredIds.clear();
    pool = [...questions];
  }
  const filtered = pool.length > 1 ? pool.filter((question) => question.id !== excludeId) : pool;
  return filtered[Math.floor(Math.random() * filtered.length)];
}

function setQuestion(question) {
  stopResultTimer();
  state.currentQuestion = question;
  state.mode = "question";
  state.result = null;
  nodes.prompt.textContent = question.prompt;
  nodes.answer.textContent = question.answer;
  nodes.difficulty.textContent = question.difficulty === "easy" ? "Easy" : "Medium";
  nodes.position.textContent = `Question ${state.answeredIds.size + 1}`;
  nodes.outcomePanel.className = "outcome-panel neutral hidden";
  nodes.outcomeLabel.textContent = "";
  nodes.outcomeValue.textContent = "";
  nodes.answer.closest(".answer-panel").classList.remove("hidden");
  nodes.startTimerButton.classList.remove("hidden");
  nodes.correctButton.classList.remove("hidden");
  nodes.wrongButton.classList.remove("hidden");
  nodes.skipButton.classList.remove("hidden");
  nodes.nextButton.classList.add("hidden");
  setQuestionTimerIdle();
}

function showReward() {
  const seconds = rewardFor(Math.max(1, state.goodStreak));
  nodes.outcomePanel.className = "outcome-panel good";
  nodes.outcomeLabel.textContent = "Current reward";
  nodes.outcomeValue.textContent = `${seconds}s`;
  state.result = { type: "reward", duration: seconds, label: "Current reward", value: `${seconds}s` };
}

function showPunishment() {
  const punishment = punishmentFor(Math.max(1, state.badStreak));
  nodes.outcomePanel.className = "outcome-panel bad";
  nodes.outcomeLabel.textContent = "Punishment";
  nodes.outcomeValue.textContent = `Intensity ${punishment.intensity} / ${punishment.length}s`;
  state.result = {
    type: "punishment",
    duration: punishment.length,
    label: "Punishment",
    value: `Intensity ${punishment.intensity} / ${punishment.length}s`,
  };
}

function renderHeader() {
  const easyCount = questions.filter((question) => question.difficulty === "easy").length;
  const mediumCount = questions.length - easyCount;
  nodes.bankSummary.textContent = `${questions.length} questions: ${easyCount} easy, ${mediumCount} medium`;
  nodes.progress.textContent = `${state.answeredIds.size} / ${questions.length}`;
}

function nextQuestion() {
  renderHeader();
  setQuestion(randomQuestion(state.currentQuestion && state.currentQuestion.id));
}

function showResultScreen() {
  state.mode = "result";
  stopTimer();
  nodes.timer.textContent = "Done";
  nodes.answer.closest(".answer-panel").classList.add("hidden");
  nodes.startTimerButton.classList.add("hidden");
  nodes.correctButton.classList.add("hidden");
  nodes.wrongButton.classList.add("hidden");
  nodes.skipButton.classList.add("hidden");
  nodes.nextButton.classList.remove("hidden");
  renderHeader();
}

function gradeCorrect() {
  if (state.mode !== "question" || !state.currentQuestion) return;
  readSettings();
  playSound(settings.correctSound);
  state.answeredIds.add(state.currentQuestion.id);
  state.goodStreak += 1;
  state.badStreak = 0;
  showReward();
  showResultScreen();
}

function gradeWrong() {
  if (state.mode !== "question" || !state.currentQuestion) return;
  readSettings();
  playSound(settings.wrongSound);
  state.answeredIds.add(state.currentQuestion.id);
  state.badStreak += 1;
  state.goodStreak = 0;
  showPunishment();
  showResultScreen();
}

function skipQuestion() {
  if (state.mode !== "question") return;
  readSettings();
  setQuestion(randomQuestion(state.currentQuestion && state.currentQuestion.id));
}

function updateResultCountdown(label, seconds) {
  nodes.outcomeLabel.textContent = label;
  nodes.outcomeValue.textContent = formatSeconds(seconds);
}

function runResultCountdown() {
  if (state.mode !== "result" || !state.result || state.resultTimer) return;
  readSettings();
  let phase = "lead";
  let remaining = 5;
  updateResultCountdown("Starting", remaining);
  state.resultTimer = setInterval(() => {
    remaining -= 1;
    if (remaining > 0) {
      updateResultCountdown(phase === "lead" ? "Starting" : state.result.label, remaining);
      return;
    }

    if (phase === "lead") {
      phase = "main";
      remaining = state.result.duration;
      playSound(settings.timerSound === "off" ? "bell" : settings.timerSound);
      updateResultCountdown(state.result.label, remaining);
      return;
    }

    stopResultTimer();
    nodes.outcomeLabel.textContent = "Done";
    nodes.outcomeValue.textContent = "00:00";
    playSound(settings.timerSound === "off" ? "bell" : settings.timerSound);
  }, 1000);
}

nodes.settingsForm.addEventListener("input", () => {
  readSettings();
  if (state.mode === "result" && state.badStreak > 0) {
    showPunishment();
  } else if (state.mode === "result") {
    showReward();
  }
  if (state.mode === "question") {
    setQuestionTimerIdle();
  }
});
nodes.startTimerButton.addEventListener("click", startTimer);
nodes.correctButton.addEventListener("click", gradeCorrect);
nodes.wrongButton.addEventListener("click", gradeWrong);
nodes.skipButton.addEventListener("click", skipQuestion);
nodes.nextButton.addEventListener("click", nextQuestion);
nodes.outcomePanel.addEventListener("click", runResultCountdown);
nodes.outcomePanel.addEventListener("keydown", (event) => {
  if (event.key === "Enter" || event.key === " ") {
    event.preventDefault();
    runResultCountdown();
  }
});

readSettings();
renderHeader();
setQuestion(randomQuestion());

window.__quizStats = {
  easy: questions.filter((question) => question.difficulty === "easy").length,
  medium: questions.filter((question) => question.difficulty === "medium").length,
  total: questions.length,
};
